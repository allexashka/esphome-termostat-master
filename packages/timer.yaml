# This file enables additional timer sensor for your dongle.
# Enable it via package include:
# packages:
#   timer: github://allexashka/esphome-termostat-master/packages/timer.yaml@master
# or if you like from local filesystem:
#   timer: !include packages/timer.yaml
substitutions:
  time_platform: sntp

time:
  platform: $time_platform

text_sensor:
  - platform: template
    id: wh_timer
    name: Timer
    icon: mdi:timer-outline
    entity_category: diagnostic

ewh:
  on_state:
    then:
      - lambda: id(wh_timer).publish_state(str_snprintf("%02u:%02u", 5, state.timer_hours, state.timer_minutes));