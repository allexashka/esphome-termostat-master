substitutions:
  board: esp32-c3-devkitm-1
  framework_type: esp-idf

esphome:
  build_flags:
    - -std=gnu++17
  # build_unflags больше не поддерживается в ESP-IDF
  # lib_ldf_mode игнорируется в ESP-IDF

esp32:
  board: $board
  framework:
    type: $framework_type