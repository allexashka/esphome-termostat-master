# This file enables cloud support to your dongle.
# Enable it via package include:
# packages:
#   cloud: github://allexashka/esphome-termostat-master/packages/cloud.yaml@master
# or if you like from local filesystem:
#   cloud: !include packages/cloud.yaml

rka_cloud:
  name: "Cloud"
  pair:
    name: "Cloud Pair"